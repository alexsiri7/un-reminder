sdk=34
# Unit tests must not boot the production Application: its onCreate builds the Hilt graph,
# enqueues WorkManager jobs and launches geofence registration on Dispatchers.IO, which
# then reaches Sentry static mocks from unrelated tests seconds later.
application=android.app.Application
